#!/usr/bin/env bash
# uetool launcher (macOS / Linux). Add this folder to your PATH.
# Set UETOOL_PYTHON to a Python 3.11+ interpreter if `python3` is older.
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${UETOOL_PYTHON:-python3}" "$DIR/uetool.py" "$@"
